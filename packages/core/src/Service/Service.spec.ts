import "reflect-metadata";
import { ServiceBroker, ServiceActionsSchema } from "moleculer";

import { Service } from "./Service";

describe("Service", () => {
    it("should build service schema with actions from metadata", () => {
    });
});
